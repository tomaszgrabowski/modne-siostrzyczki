import { PasswordEqualDirective } from './password-equal.directive';

describe('PasswordEqualDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordEqualDirective();
    expect(directive).toBeTruthy();
  });
});
