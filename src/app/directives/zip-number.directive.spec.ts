import { ZipNumberDirective } from './zip-number.directive';

describe('ZipNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ZipNumberDirective();
    expect(directive).toBeTruthy();
  });
});
