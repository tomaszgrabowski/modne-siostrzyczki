import { SizesFiedDirective } from './sizes-fied.directive';

describe('SizesFiedDirective', () => {
  it('should create an instance', () => {
    const directive = new SizesFiedDirective();
    expect(directive).toBeTruthy();
  });
});
